# bulk_http

A massive asynchronous, multiprocess HTTP execution engine for Python.

`bulk_http` streams, sends, filters and persists millions of HTTP requests with
memory usage that stays constant regardless of input size, browser fingerprint
impersonation, proxy management and crash-safe resumption.

> Status: early development.
